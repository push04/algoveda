fix: Fix build errors, homepage ticker, and logged-in user greeting

- Add SupabaseClient type import to fix build error in API routes
- Make TickerBar fixed at top, prevent gap on scroll
- Add padding to account for fixed ticker
- Show "Hi, {name}" on homepage for logged-in users
- Fix nav to be full-width instead of fixed width

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>